# 17 [COMMAND]
# Find the command that allows you to connect via ssh on the VM.(In parallel with the graphic session)

	ssh -p 3022 cschulle@127.0.0.1	
	# must be enabled using port forwarding; can do this in VirtualBox

